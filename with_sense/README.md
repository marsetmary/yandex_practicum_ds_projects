# Поиск релевантных изображений по текстовому описанию

**Цель проекта:** создать модель, которая по текстовому описанию находит релевантные изображения. Модель должна выдавать число от 0 до 1, показывающее степень соответствия текста и изображения, а также исключать материалы с детьми младше 16 лет.

---

## Задачи проекта
- Загрузка и исследовательский анализ данных  
- Подготовка данных к обучению  
- Обучение моделей  
- Тестирование и демонстрация работы модели  
- Формирование итогового вывода и рекомендаций

---

## Основные результаты
- Загружены и проверены все данные: train_df, test_df, crowd_df, expert_df, query_df  
- Создан объединённый датафрейм с нормализацией оценок экспертов и краудсорсинга (приоритет — экспертные оценки) для обучения  
- Проведена токенизация и лемматизация текстов; удалены стоп-слова; отфильтрованы изображения с детьми младше 16 лет (14963 строки)  
- Извлечены эмбеддинги текста (BERT all-mpnet-base-v2) и изображений (ResNet18 без последнего слоя)  
- Разделение данных на тренировочную и валидационную выборки с использованием GroupShuffleSplit  
- Обучены модели: Linear Regression, Gradient Boosting с подбором гиперпараметров, нейросеть с одним скрытым слоем, dropout=0.4, lr=0.001, ReLU, MSELoss  
- **Лучшая модель:** нейросеть, RMSE на валидации = 0.193  
- Для тестирования реализована функция `get_pictures`, которая объединяет эмбеддинги, нормализует их, получает предсказания и возвращает топ-подходящее изображение с оценкой соответствия  
- Модель корректно фильтрует запрещённый контент, различает людей и животных, распознаёт действия (играть в футбол, на инструменте, сидеть и т.д.)

---

## Выводы
- Подход с объединением текстовых и визуальных эмбеддингов эффективен  
- Нейросеть успешно выучила соответствие описаний и изображений при соблюдении ограничений по безопасности контента  
- Результаты демонстрируют высокую релевантность поиска и гибкость модели к различным типам запросов  
---

## Используемые библиотеки
pandas, numpy, matplotlib, seaborn, torch, torchvision, transformers, spacy, nltk, PIL, sklearn